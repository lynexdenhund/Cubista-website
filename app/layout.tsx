import type { Metadata, Viewport } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  weight: ["400", "500", "600", "700"],
})

export const metadata: Metadata = {
  title: "Cubista - Unlock Your Relationship Data",
  description:
    "Uncover opportunities, strengthen connections, and drive growth - all in one platform. Cubista centralizes relationship signals into one living context layer.",
}

export const viewport: Viewport = {
  themeColor: "#1e40af",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  )
}
